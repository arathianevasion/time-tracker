# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local sqlite data
/data/

# packaged launcher zip (scripts/make-launcher.sh)
/dist/

# in case the launcher is ever double-clicked from inside a repo checkout instead of next to one
/time-tracker/

# Andy's personal migration data (real Jira baseline + worklog IDs) — local only, never committed
/scripts/seed.ts
/src/lib/db/seed.ts
/reference/
